# Lattice C 5.00 A4 fixture

`root.c` is version-local source for comparison with later Lattice releases.
It is built through the release's own `lc1`, `lc2`, and BLINK binaries.  The
generated object and executable are retained once validation completes;
temporary `.q` phase files are not.

The checked executable SHA-256 is:

```text
44CA9F9BADEFD085E5EF514B216277365819697B96DECF7705B477937BEFD3A2
```
